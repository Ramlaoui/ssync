import type { Keyboard } from "@raycast/api";
export const WATCHERS_SHORTCUT = {
  modifiers: ["cmd", "shift"],
  key: "w",
} satisfies Keyboard.Shortcut;
export const OUTPUT_SHORTCUT = {
  modifiers: ["cmd"],
  key: "o",
} satisfies Keyboard.Shortcut;
export const SCRIPT_SHORTCUT = {
  modifiers: ["cmd", "shift"],
  key: "s",
} satisfies Keyboard.Shortcut;
export const HOSTS_SHORTCUT = {
  modifiers: ["cmd", "shift"],
  key: "h",
} satisfies Keyboard.Shortcut;
export const CONNECTIONS_SHORTCUT = {
  modifiers: ["cmd", "shift"],
  key: ",",
} satisfies Keyboard.Shortcut;
export const PIN_SHORTCUT = {
  modifiers: ["cmd", "shift"],
  key: "p",
} satisfies Keyboard.Shortcut;
export const INSPECTOR_SHORTCUT = {
  modifiers: ["cmd", "shift"],
  key: "d",
} satisfies Keyboard.Shortcut;
export const RELAUNCH_SHORTCUT = {
  modifiers: ["cmd", "shift"],
  key: "l",
} satisfies Keyboard.Shortcut;
