host_partition: example-gpu
env: project
prepare:
  scripts:
    - .ssync/fragments/login/prefetch_dataset.sh
run:
  script: .ssync/fragments/run/train.sh
vars:
  MAX_STEPS: "100000"
watchers:
  - timeout_resume
